pull_request_rules:
  - name: VM5 private journal generation fixture
    conditions:
      - title = VM5_JOURNAL_PRIVATE_GENERATION_260923
      - base = main
    actions:
      queue:

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = vm5-journal-never
    merge_method: merge
